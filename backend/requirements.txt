# ============================================
# MOMENTUM Backend Dependencies
# ============================================
# IMPORTANT: Install in this exact order:
#   1. pip install -r requirements.txt
#   2. pip install "numpy==1.26.4"   (to fix numpy version pulled by torchvision)
# ============================================

# --- CUDA (install first, then fix numpy) ---
torch
torchvision

# --- Core ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
qdrant-client==1.12.0
python-multipart==0.0.9
aiofiles==24.1.0

# --- CV & ML ---
opencv-python==4.10.0.84
numpy>=1.26.0,<2.0
Pillow==10.4.0
open-clip-torch==2.24.0
ultralytics==8.2.0
